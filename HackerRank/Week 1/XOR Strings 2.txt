XOR Strings 2

#include <stdio.h>

int main() {
    char a[100], b[100];
    scanf("%s", a);
    scanf("%s", b);
    int n = strlen(a);
    for(int i=0; i<n; i++){
        if(a[i]==b[i]){
            printf("0");
        }
        else{
            printf("1");
        }
    }
    
    return 0;
}
