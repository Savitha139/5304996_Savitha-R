Drawing book

#include <stdio.h>
int pageCount(int n, int p) {
     int a = p/2;
     int b = (n/2)-(p/2);
     return a < b ? a : b;
}
int main() {
    int n, p;
    scanf("%d %d", &n, &p);
    int r = pageCount(n, p); 
    printf("%d\n", r); 
    return 0;
}